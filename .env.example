# Ejemplo de configuración de variables de entorno para despliegue
# NO incluir este archivo en el repositorio con valores reales

# =============================================================================
# CONFIGURACIÓN BÁSICA
# =============================================================================

# Entorno de ejecución
ENVIRONMENT=production

# Puerto del servidor (Railway configura esto automáticamente)
PORT=8000

# =============================================================================
# FIREBASE/FIRESTORE CONFIGURATION
# =============================================================================

# MÉTODO 1: WORKLOAD IDENTITY FEDERATION (RECOMENDADO)
# ID del proyecto Firebase (Recomendado pero opcional si WIF puede detectarlo)
FIREBASE_PROJECT_ID=tu-proyecto-firebase-id

# Credenciales WIF en JSON (PRIMERA OPCIÓN - MÁS SEGURA)
GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"external_account","audience":"//iam.googleapis.com/projects/123456789/locations/global/workloadIdentityPools/railway-pool/providers/railway-provider",...}

# MÉTODO 2: SERVICE ACCOUNT KEY (FALLBACK)
# Clave del Service Account en base64 (SOLO si no usas WIF)
# Obtener con: base64 -w 0 service-account-key.json
# FIREBASE_SERVICE_ACCOUNT_KEY=eyJ0eXBlIjoic2VydmljZV9hY2NvdW50Ijoi...

# Configuración adicional de Firestore
FIRESTORE_BATCH_SIZE=500
FIRESTORE_TIMEOUT=30

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# URL del frontend en producción
FRONTEND_URL=https://tu-app-frontend.vercel.app

# Orígenes adicionales separados por coma
CORS_ORIGINS=https://otro-dominio.com,https://otro-subdominio.app.com

# =============================================================================
# SEGURIDAD Y AUTENTICACIÓN
# =============================================================================

# Dominio autorizado para autenticación Google
AUTHORIZED_EMAIL_DOMAIN=@tu-organizacion.com

# Configuración JWT (opcional)
JWT_SECRET_KEY=tu-clave-secreta-muy-larga-y-segura

# =============================================================================
# BASE DE DATOS Y CACHE (opcional)
# =============================================================================

# Redis para cache (si se implementa)
REDIS_URL=redis://localhost:6379

# =============================================================================
# LOGGING Y MONITOREO
# =============================================================================

# Nivel de logging
LOG_LEVEL=INFO

# Configuración de métricas (opcional)
ENABLE_METRICS=false