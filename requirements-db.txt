# ============================================
# V3 MIGRATION — POSTGRESQL + POSTGIS DATA STACK
# ============================================
# Additive stack for the Firestore -> PostgreSQL/PostGIS migration (v3).
# Kept separate from requirements.txt (the Firebase/Railway prod stack) so the
# production deploy is not affected until the flip. shapely is already pinned in
# requirements.txt and is reused here.
#
# Exact versions frozen from the local Python 3.12 venv (pip 3.12.10).

# ORM + async driver
SQLAlchemy[asyncio]==2.0.51
asyncpg==0.31.0
greenlet==3.5.3            # required by SQLAlchemy asyncio

# PostGIS geometry types for SQLAlchemy + Alembic
GeoAlchemy2==0.20.0

# Schema migrations
alembic==1.18.5
Mako==1.3.12               # alembic template engine

# Settings (core_db/settings.py)
pydantic-settings==2.14.2

# Integration tests against a real PostGIS container
testcontainers==4.14.2
